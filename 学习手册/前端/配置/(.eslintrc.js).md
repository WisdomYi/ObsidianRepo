```javascript
module.exports = {
  // 环境：指定代码运行的环境（浏览器/Node.js，会自动启用对应的全局变量）
  env: {
    browser: true, // 浏览器环境
    es2021: true,  // ES2021 特性
    node: true     // Node.js 环境
  },
  // 继承：复用已有的规则集（可继承多个，后定义的覆盖前的）
  extends: [
    "eslint:recommended", // ESLint 官方推荐规则
    "plugin:react/recommended", // React 推荐规则
    "plugin:react-hooks/recommended", // React Hooks 规则
    "plugin:@typescript-eslint/recommended", // TypeScript 推荐规则
    "prettier" // 与 Prettier 兼容（需安装 eslint-config-prettier）
  ],
  // 解析器：指定代码解析器（TS 项目需用 @typescript-eslint/parser）
  parser: "@typescript-eslint/parser",
  // 解析器选项：配置解析器的行为
  parserOptions: {
    ecmaFeatures: {
      jsx: true // 支持 JSX
    },
    ecmaVersion: "latest", // 支持最新 ES 语法
    sourceType: "module" // 模块类型（ES modules）
  },
  // 插件：引入额外的规则集（需先安装对应插件）
  plugins: [
    "react",
    "react-hooks",
    "@typescript-eslint"
  ],
  // 规则：自定义检查规则（覆盖继承的规则）
  // 规则值："off"（关闭）、"warn"（警告）、"error"（错误，阻断构建）
  rules: {
    // 关闭 React 版本检测（避免警告）
    "react/react-in-jsx-scope": "off",
    // 强制使用驼峰命名
    "camelcase": "error",
    // 禁止未使用的变量（TS 项目建议用 @typescript-eslint 的规则）
    "@typescript-eslint/no-unused-vars": ["error", { vars: "all", args: "after-used" }],
    // 允许 console（开发环境）
    "no-console": process.env.NODE_ENV === "production" ? "warn" : "off"
  },
  // 全局变量：声明项目中使用的全局变量（避免报错）
  globals: {
    React: "writable"
  },
  // 忽略特定文件/目录的检查（也可单独通过 .eslintignore 文件配置）
  ignorePatterns: ["dist/", "node_modules/", "*.config.js"]
};
```